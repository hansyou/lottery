# 🎉 婚礼抽奖工具

## 启动

```bash
node server.js
```

访问 http://localhost:6688

## 人员名单

在项目根目录创建 `namelist.txt`，启动后自动加载。支持换行或逗号分隔：

```
张三
李四,王五
赵六, 孙七, 周八
```

## 使用

1. 页面加载后，`namelist.txt` 中的名单会自动填入
2. 设置奖项名称和数量
3. 点击"开始抽奖"，依次抽完所有奖项
4. 抽奖完成后可下载 `result.txt` 保存结果
